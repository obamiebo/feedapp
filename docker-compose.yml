services:
  postgres:
    image: postgres:16-alpine
    container_name: feedback-hub-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: feedback
      POSTGRES_PASSWORD: feedback
      POSTGRES_DB: feedback_hub
    ports:
      - "5432:5432"
    volumes:
      - feedback_hub_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U feedback -d feedback_hub"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  feedback_hub_postgres_data:
